# 確認事項リスト

> 最終更新: 2026-09-08（第2回MTG 2026-09-07 の内容を反映）
> 出典: `docs/要件定義書.md` §9・§10 ＋ 第2回ミーティング議事録 ＋ 実装時に発生した論点
> 実装で埋めた値のうち **推測で置いたものはすべてここに載せる**。サイト側では
> `要確認` / `仮置き・要確認` のバッジで見えるようにしてある（`grep -rn "tbd" public/`）。

## ⚠️ 前提が変わった：法人 → 個人事業主

**2026-08-31 で法人格が消滅し、武田様は個人事業主になった。**（第2回MTGで本人が言及）
メールアドレスも変わっている。したがって **「UNITE税理士法人」は現時点で誤り**。

サイト全体を **「武田税理士事務所」で仮置き** してある。正式な屋号は未回収のため、
事務所概要とプライバシーポリシーの冒頭に「仮置きである」旨を明示している。
確定しだい `grep -rn "武田税理士事務所" public/ src/` で一括置換する。

## A. 武田様に確認するもの（第3回MTG 2026-10-01 14:00）

| # | 項目 | サイト上の現在値 | 該当箇所 |
|---|---|---|---|
| A-1 | **事務所の正式名称** | `武田税理士事務所`（**仮置き**） | `public/index.html` ヘッダー・事務所概要・フッター、`public/privacy.html`、`public/404.html`、`<title>`・OGP |
| A-2 | 代表者氏名・肩書 | `武田 敏弘`（肩書なし） | 事務所概要 |
| A-3 | 所在地 | `〒000-0000（住所）` | 事務所概要、プライバシーポリシー §10 |
| A-4 | 電話番号 | `00-0000-0000` | 事務所概要、プライバシーポリシー §10 |
| A-5 | **メールアドレス**（個人事業主化で変更済み） | `（メールアドレス）` | 事務所概要、プライバシーポリシー §10 |
| A-6 | 受付時間 | `平日 9:00 - 18:00` | 事務所概要 |
| A-7 | 開業年月（法人ではないので「設立」から改称した） | `0000年0月` | 事務所概要 |
| A-8 | 掲載サービスの一覧と並び順 | BASE 3件（税務顧問／記帳・月次決算／決算・申告）＋ GROWTH 3件（財務コンサル／経営コンサル／社長個人の資産設計） | `#service` |
| A-9 | 事例の数値4件 | 参考デザインの数値をそのまま使用（+35% / +2.8pt / +2,000万円 / +28%） | `#case` |
| A-10 | ドメイン名 | 未取得。暫定URLは `takedasite.souzohnic.workers.dev` | — |

**A-8 は「こちらで仮置きし、修正事項として受ける」で菊池が合意済み**（第2回MTG）。
**A-9 はダミーであることを画面上に明示済み**（セクション冒頭の注意書き＋各カードのバッジ＋脚注）。
掲載できる事例が無い場合はセクションごと削除する選択肢も提示してある。

## B. 武田様側で用意いただくもの

| # | 項目 | 状況 |
|---|---|---|
| B-1 | Cloudflare アカウントの作成 | **完了**（第2回MTG）。菊池を管理者権限で招待済み・承諾済み |
| B-2 | microCMS アカウントの作成・招待 | **完了**（第2回MTG）。菊池を管理者で招待済み・承諾済み |
| B-3 | Lark Base とカスタムアプリの作成 | **完了**（第2回MTG）。bitable スコープ付与・リリース済み、Base にアプリを編集権限で追加、菊池を管理可能で招待済み。**App ID / App Secret は Lark グループに送付済み** |
| B-4 | プライバシーポリシーのリーガルチェック | 叩き台を `public/privacy.html` に作成済み。10/1 まで |
| B-5 | 修正点のまとめ | 9/19〜10/1。菊池作成の不具合修正報告フォーム経由 |

## C. ソウゾウ側で残っている作業（9/18 の構築完了まで）

| # | 項目 | 状況 |
|---|---|---|
| C-1 | Lark Base の問い合わせテーブルの列作成 | **完了**（2026-09-08）。武田様の Base に8列作成済み |
| C-2 | Lark の資格情報を Worker シークレットに設定 | **完了**（2026-09-08）。4つとも設定済みで、実送信テストも通っている |
| C-3 | microCMS の「お知らせ」API 作成と接続 | **完了**（2026-09-08）。エンドポイント `news`、フィールドは `title` / `url`。サイトに実際に表示されることを確認済み |
| C-4 | 武田様の Cloudflare アカウントへデプロイ移管 | **未**。`wrangler whoami` にまだ武田様のアカウントが出ないため、OAuth の取り直しが必要 |
| C-5 | ドメイン取得・DNS・SSL（Cloudflare 上で購入する方針） | A-10 待ち |
| C-6 | 事務所概要の `要確認` バッジを外す | A-1〜A-7 待ち |
| C-7 | プライバシーポリシー冒頭の注記を削除 | 公開直前 |
| C-9 | microCMS のテストコンテンツ（タイトル「テスト」）を消すか実際の文言に差し替える | 9/18 の提出前に |
| C-10 | 武田様の microCMS 権限を「お知らせの編集」に限定する | 菊池さんが管理者なので後から設定可能 |
| C-8 | 対応ステータス管理・対応期日の自動通知 | **今回は対象外。**第3回MTGで決めると菊池が明言（Base構築＋送信までが今回のスコープ） |

### ✅ Lark 連携は 2026-09-08 に完了

武田様の Base（`SGX9bDklBa0dZGsUCuujt1pKpkg` / テーブル `tblXhWQ6LKqb7wgY`）に
8列（受付日時／種別／会社名／お名前／メールアドレス／ご相談内容／ステータス／添付ファイル）を
作成し、本番サイトのフォームから実際に送信して端から端まで確認した。

- `/api/contact` が `{"ok":true,"stored":true,"delivered":true}` を返す
- Base に8列すべてが正しく入る。**添付ファイルのアップロードも通る**
- 確認に使ったレコードと KV の控えは削除済み（KV は 0 件）

武田様の Lark は **日本リージョンの larksuite**（Base の URL が `*.jp.larksuite.com`）。
API ホストは `https://open.larksuite.com` のままでよい。

テーブルの列を変える場合は `scripts/setup-lark-base.mjs` の `FIELDS` と
`src/worker.js` の `sendToLark()` を必ず両方直すこと。

残る C-8（ステータス管理・対応期日の自動通知）は Lark Base 側の設定作業で、
第3回MTG（10/1）で仕様を決めてから着手する。

**問い合わせは Lark Base と Cloudflare KV の両方に入る。**
Lark 側が落ちていても KV に控えが残るので取りこぼさない。KV の中身は次で確認する。

```bash
npx wrangler kv key list --namespace-id 686fa302084e4bacaf163180b0433776 --remote
npx wrangler kv key get "inq:<キー>" --namespace-id 686fa302084e4bacaf163180b0433776 --remote
```

## D. 実装時に自分で決めた（合意を取っていない）こと

| # | 決めたこと | 理由 |
|---|---|---|
| D-1 | アクセントカラーを参考デザインより濃い `#4a7268` にした | 参考デザインの明るいセージ（概ね `#7ba296`）では白文字とのコントラストが 2.8:1 で、本文が読みにくかった。濃度を上げて 5.3:1（WCAG AA）を確保している。**色味を参考デザインに戻す場合は、帯の文字色を黒にする必要がある** |
| D-2 | ヒーローに抽象グラフのSVGを置いた | 写真を使わない方針のため。参考デザインの構図を踏襲 |
| D-3 | `FLOW`（ご相談の流れ）セクションを追加した | 参考デザインには無い。無料診断の心理的ハードルを下げるため。不要なら削除する |
| D-4 | 事例をCMS化せず固定掲載にした | 要件定義書 §3 の合意事項どおり |
| D-5 | 添付ファイルの上限を10MBにした | 決算書PDFなら十分。Lark のアップロードAPIの制約ではない |
| D-6 | 名称を「武田税理士事務所」で仮置きした | 個人事業主化で「UNITE税理士法人」が使えなくなったため。正式な屋号は未回収。菊池の「仮置きでやっちゃっていい」方針に沿う |
| D-7 | 「設立」を「開業」に変更した | 個人事業主なので「設立」は不自然 |
